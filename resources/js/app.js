import './bootstrap';

// Global CRMTracker namespace
window.CRMT = window.CRMT || {};

// Initialize when DOM is ready
document.addEventListener('DOMContentLoaded', () => {
    console.log('CRMTracker app initialized');
});
